const nombre = "Bryan";
const apellido = "Ticona";
const misdatos = { nombre, apellido };

// sessionStorage.setItem("misdatos", JSON.stringify(datos))
// localStorage.setItem("datos", JSON.stringify(datos))

const ahora = new Date();

// document.cookie = `datos=${JSON.stringify(datos)};expires=${new Date(
//   ahora.getTime() + 2 * 60000
// )}`;
